<header class="navbar navbar-expand-md d-none d-lg-flex d-print-none">
    <div class="container-xl">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar-menu">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="navbar-nav flex-row order-md-last">
            <div class="d-none d-md-flex me-3">
                <a href="{{ path('app_home') }}" class="btn btn-outline-primary btn-sm" target="_blank">
                    <i class="fa-solid fa-external-link-alt me-1"></i> Voir le site
                </a>
            </div>
            
            <div class="nav-item dropdown">
                <a href="#" class="nav-link d-flex lh-1 text-reset p-0" data-bs-toggle="dropdown" aria-label="Options utilisateur">
                    <span class="avatar avatar-sm bg-primary-lt">
                        {{ app.user.name|first|upper }}
                    </span>
                    <div class="d-none d-xl-block ps-2">
                        <div>{{ app.user.displayName }}</div>
                        <div class="mt-1 small text-muted">Administrateur</div>
                    </div>
                </a>
                <div class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                    <a href="{{ path('app_profile') }}" class="dropdown-item">
                        <i class="fa-solid fa-user me-2"></i> Mon profil
                    </a>
                    <div class="dropdown-divider"></div>
                    <a href="{{ path('admin_logout') }}" class="dropdown-item text-danger">
                        <i class="fa-solid fa-sign-out-alt me-2"></i> Déconnexion
                    </a>
                </div>
            </div>
        </div>
        
        <div class="collapse navbar-collapse" id="navbar-menu">
            <div class="d-flex flex-column flex-md-row flex-fill align-items-stretch align-items-md-center">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_dashboard') }}">
                            <span class="nav-link-icon d-md-none d-lg-inline-block">
                                <i class="fa-solid fa-gauge-high"></i>
                            </span>
                            <span class="nav-link-title">Tableau de bord</span>
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</header>
